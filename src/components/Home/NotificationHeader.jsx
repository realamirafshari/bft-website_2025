import React from "react";

const NotificationHeader = () => {
  return (
    <section className="bg-secondary-light  text-secondary-main text-center font-semibold px-8 py-3 md:py-4 flex justify-center items-center transition-all">
      Warm Up Version is now available! Read about the new features and fixes from
      November.
    </section>
  );
};

export default NotificationHeader;
